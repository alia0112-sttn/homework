// Метод POP====>

const num = [1, 2, 3, 4, 5, 'alex', 'tom'];

const notNum = num.pop();

console.log(notNum);
console.log(num)

// Метод shift ====>

    const nam = ['alex', 'john', 'max','tomas', 'anna'];

    const firstNam = nam.shift();

    console.log(firstNam);
    console.log(nam)

    // Метод unshift====>

        const age = [19, 20, 21, 22];

        const total = age.unshift(17, 18);

        console.log(total);
        console.log(age)